---

---

<div class="relative flex flex-col gap-4">
    <div>
        <p>Enrique Krahe</p>
        <p>Graphic designer based in Madrid.</p>
    </div>
    <div>
        <p class="text-xs">Contact</p>
        <p><a href="https://www.instagram.com/enrikrahe/">@enrikrahe</a></p>
        <p><a href="mailto:enrique-krahe@proton.me">enrique-krahe@proton.me</a></p>
    </div>
    <div>
        <p class="text-xs">Clients</p>
        <ul>
            <li>
                <a href="https://www.instagram.com/dazzzzzzzzed/">DAZZZED Studio</a>
            </li>
            <li>
                <p>Willem de Kooning Academy</p>
            </li>
        </ul>
        <div>
            <button id="size-toggle" class="absolute right-0 bottom-0 hover:opacity-60 transition-opacity cursor-pointer">M</button>
        </div>
    </div>
</div>

<script is:inline>
    document.addEventListener("DOMContentLoaded", () => {
        const sizeButton = document.getElementById("size-toggle");
        const sizes = ["S", "M", "L", "XL"];
        const sizeHeights = {
            "S": 180,
            "M": 300,
            "L": 450,
            "XL": 580
        };

        let currentSizeIndex = 1;

        sizeButton?.addEventListener("click", (e) => {
            e.preventDefault();
            currentSizeIndex = (currentSizeIndex + 1) % sizes.length;
            const newSize = sizes[currentSizeIndex];
            sizeButton.textContent = newSize;

            window.dispatchEvent(new CustomEvent("sliderSizeChange", {
                detail: {
                    size: newSize,
                    height: sizeHeights[newSize]
                }
            }));
        });
    });
</script>